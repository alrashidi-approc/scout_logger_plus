# Copy to your app root as `.env` — never commit real keys.
# Paste the full DSN from the scout dashboard (host, port, project id).
SCOUT_DSN=http://sk_live_YOUR_KEY@your-host:PORT/your-project-id

# Optional — overrides ScoutOptions.environment (default: production)
# SCOUT_ENVIRONMENT=development
