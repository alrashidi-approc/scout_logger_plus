# Copy to example/.env — never commit real keys
# Paste the full DSN from the scout dashboard
SCOUT_DSN=http://sk_live_YOUR_KEY@your-host:PORT/your-project-id
SCOUT_ENVIRONMENT=development

# Optional — demo API base URL
API_BASE_URL=https://httpbin.org
